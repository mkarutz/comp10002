#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Your code here: give declarations that reflect the data scenario that is 
   described. */

int calculate_avg_age_death(person_t *country, int num_people);
int calculate_tax_revenue(person_t *country, int num_people, int year);

int 
main(int argc, char *argv[]) {
	/* Your code here: write some tests for your functions. */
	return 0;
}

/* Calculates, for a specified country, the average age of death. Does not 
   include people that are not yet dead. */
int 
calculate_avg_age_death(person_t *country, int num_people) {
	/* Your code here. */
	return 0;
}

/* Calculates, for a specified country, the total taxation revenue in a 
   specified year. */
int 
calculate_tax_revenue(person_t *country, int num_people, int year) {
	/* Your code here. */
	return 0;
}
